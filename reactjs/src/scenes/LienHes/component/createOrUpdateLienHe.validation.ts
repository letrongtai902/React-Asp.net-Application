import { L } from '../../../lib/abpUtility';

const rules = {
    diaChi: [{ required: true, message: L('ThisFieldIsRequired') }],
    sdt: [{ required: true, message: L('ThisFieldIsRequired') }],
};

export default rules;